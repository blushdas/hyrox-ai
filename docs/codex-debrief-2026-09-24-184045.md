# Codex Implementation Debrief

## Source Handoff
- Handoff: docs/codex-handoff-2026-09-24.md
- Goal: docs/codex-goal-2026-09-24.md
- Branch: codex/finisher-landing-split
- Date: 2026-09-24
- Supersedes: none

## Original Goal
Remove the app repository's marketing components and waitlist endpoint/data; replace the root with a mounted client redirect to /dashboard while preserving app routes.

## What Was Implemented
PARTIAL: Root now mirrors scripts/ios-app-entry.tsx, using two effects, mounted state, router.replace("/dashboard"), and null rendering. Updated obsolete header navigation and landing smoke checks to exercise root entry and the existing sign-in gate.

Deletion remains pending the exact user approval phrase required by supplied AGENTS.md: “No bulk deletes (files, emails, repos, data) without user saying CODE BLACK approved.” Requested approval through the user-input tool; no response received before this checkpoint. No file has been deleted.

## Files Changed
- src/app/page.tsx: root client redirect.
- e2e/navigation.spec.ts: replace all three obsolete landing-header cases with desktop/mobile root auth-flow and direct sign-in checks. All three cases exercised the removed root UI; none covered unrelated app navigation.
- out: e2e/landing.spec.ts: supporting edit required because its old headline, alpha badge, CTA, and header assertions target root UI that no longer renders. Retain title and console smoke checks with updated expectations.
- docs/qa/finisher-landing-split-2026-09-24/*.png: browser evidence.
- This debrief: partial implementation checkpoint.

## Goal Coverage
| # | Acceptance Criterion | Status | Evidence-Type | Evidence |
| --- | --- | --- | --- | --- |
| R1 | Root redirects client-side to dashboard using the mounted reference pattern | ⚠️ Partial | runtime | Desktop and mobile root navigation ends at /sign-in?callbackUrl=%2Fdashboard through the existing proxy. R1.png and R1-mobile.png. Root pattern matches reference apart from component name. Authenticated dashboard landing not verified. |
| R2 | Landing files removed and no source imports remain | ❌ Not done | static | Source import grep is empty, but all eight landing files remain pending deletion authorization. |
| R3 | Waitlist route/data and references removed | ❌ Not done | static | Route and data still exist; grep finds form fetch and protected-routes test row. Pending deletion authorization. |
| R4 | Production build passes without new warnings | ✅ Done | build | npm run build exited 0; compiled successfully, TypeScript passed, 10/10 pages generated, no warnings. Build still includes /api/waitlist because deletion is pending. |
| R5 | Existing app routes behave unchanged | ⚠️ Partial | runtime | All six requested URL paths visited; protected paths display existing sign-in gate; screenshots R5-dashboard/plan/profile/session/onboarding/sign-in.png. Authenticated content blocked by MissingSecret. Both onboarding copies cannot be independently exercised: only src/app/onboarding/page.tsx is tracked here. |
| R6 | Dependencies and protected implementation/config untouched | ✅ Done | static | git diff empty for package.json, package-lock.json, src/auth.ts, src/proxy.ts, src/stores, capacitor.config.ts and scripts/build-ios.ts. |

## Verification
- PASS — npm ci --no-audit --no-fund: exit 0, “added 968 packages in 22s”. Existing dependency deprecation warnings for node-domexception, uuid and glob; lockfile unchanged.
- PASS — npm run build: exit 0. Actual output:
```text
▲ Next.js 16.1.6 (Turbopack)
- Environments: .env.local
Creating an optimized production build ...
✓ Compiled successfully in 3.5s
Running TypeScript ...
Collecting page data using 9 workers ...
✓ Generating static pages using 9 workers (10/10) in 167.7ms
Finalizing page optimization ...
Routes: /, /_not-found, /api/auth/[...nextauth], /api/waitlist,
/dashboard, /onboarding, /plan, /profile, /session/[id], /sign-in
Proxy (Middleware)
```
- PASS — grep -rn "components/landing" src/: no output (no matches).
- FAIL acceptance — grep -rn "api/waitlist" src/: two matches, src/components/landing/waitlist-form.tsx:24 and src/lib/auth/protected-routes.test.ts:7. Deletions not yet authorized.
- FAIL acceptance — ls src/components/landing: app-preview.tsx, cta-section.tsx, features.tsx, footer.tsx, header.tsx, hero.tsx, social-proof.tsx, waitlist-form.tsx.
- FAIL acceptance — ls src/app/api/waitlist 2>&1: “route.ts”.
- FAIL acceptance — ls data/waitlist.json 2>&1: “data/waitlist.json”.
- PASS — npx playwright test e2e/navigation.spec.ts: “PASS (3) FAIL (0)”.
- PASS — npx playwright test e2e/navigation.spec.ts e2e/landing.spec.ts: “5 passed (3.5s)”. Runner warns that NO_COLOR is ignored because FORCE_COLOR is set.
- PASS — npm test: “Test Files 2 passed (2)”, “Tests 23 passed (23)”.
- PASS — git diff --check: no output.
- PASS — npm run dev: Next.js 16.1.6, localhost:3000, “Ready in 6.1s”. Server started from this worktree and stopped after QA.
- PARTIAL — Built-in browser visited /, /dashboard, /plan, /profile, /session/w1-d1, /onboarding, /sign-in. Each protected path reached /sign-in with its correct callbackUrl. Console warnings/errors empty across those visits. R1 mobile tested at 390×844. Existing session ID w1-d1 confirmed in src/lib/hyrox-plan-data.ts.
- BLOCKED auth-content verification — clicked visible “Continue with Google”. Actual result /sign-in?error=Configuration; GET /api/auth/providers 500. Console: “ClientFetchError: There was a problem with the server configuration. Check the server logs for more information.” Server: “[auth][error] MissingSecret: Please define a `secret`.” Screenshot R5-auth-blocked.png. QA_EMAIL/QA_PASSWORD present, but provider flow fails before credential entry; no credentials exposed or fabricated.
- Static manifest search found e2e/landing.spec.ts beyond the handoff targets; updated because redirect otherwise makes its marketing assertions invalid.

## Known Gaps / Risks
- Expected environment blockers: none in handoff.
- New AUTH_CONFIG_MISSING: local AUTH_SECRET absent; actual sign-in attempt fails with MissingSecret. Authenticated dashboard, plan, profile, session and onboarding content remain unverified.
- New APPROVAL_REQUIRED: exact CODE BLACK approved response pending for ten handoff deletion targets. This is an AGENTS.md constraint, not an automatic approval rejection.
- Product risk: waitlist endpoint remains accessible until deletion resumes.
- Handoff factual conflict: existing src/proxy.ts does gate app routes, contrary to the handoff's “no server-side auth gate” statement. Preserved proxy exactly; root routes toward dashboard, then unauthenticated users correctly reach sign-in.
- No tracked second onboarding copy found in this worktree. Did not add or consolidate routes.

## Deliberately Left
- Pending requested changes: eight landing files, waitlist endpoint/data deletion, and removal of the protected-routes waitlist test row. Introduced task incompleteness, awaiting required approval.
- Pre-existing authentication configuration failure and unrelated onboarding E2E assertions that assume unauthenticated access. Not changed.
- Standalone landing repository, hosting and domain work remain outside scope.

## Falsification Test
Weakest behavior evidence: existing authenticated app routes. Configure a working local auth secret/provider environment, sign in using QA credentials, generate or restore a plan through the existing flow, then load / and verify /dashboard remains rendered. Navigate plan, profile, session and onboarding through visible controls, inspect console/network and capture each route. A sign-in loop, render error, or route regression disproves coverage.

## Claude Pickup Notes
1. Obtain CODE BLACK approved; delete only the ten listed targets and remove the stale waitlist test row. R2/R3 are not complete; this checkpoint is not ready to ship.
2. Resolve local MissingSecret through environment configuration, then repeat authenticated R1/R5 browser QA. Preserve auth/proxy implementation.
3. Re-run required build/search/file checks and affected tests after deletion, supersede this debrief with final evidence, then run /checkit.

## Suggested /checkit Inputs
- [ ] Handoff: docs/codex-handoff-2026-09-24.md
- [ ] Goal: docs/codex-goal-2026-09-24.md
- [ ] Treat this debrief as a partial checkpoint; approval and authenticated QA remain outstanding.
- [ ] Inspect docs/qa/finisher-landing-split-2026-09-24/ screenshots.
